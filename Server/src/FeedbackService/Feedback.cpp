#include"Feedback.h"

    int Feedback::getFeedbackId() const {
        return feedbackId;
    }

    double Feedback::getRating() const {
        return rating;
    }

    std::string Feedback::getComment() const {
        return comment;
    }

    std::string Feedback::getDate() const {
        return date;
    }

    int Feedback::getItemId() const {
        return itemId;
    }

